# Gobind Singh — Digital Home

An Astro-powered, Markdown-first digital home for Gobind Singh: software engineer, researcher, writer, and lifelong learner.

## Development

```sh
npm install
npm run dev
```

Content lives in `src/content`. Build with `npm run build`; the generated static site is deployed to GitHub Pages through `.github/workflows/deploy.yml`.
